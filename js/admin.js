// Fake numbers until backend is ready

document.getElementById("totalProducts").textContent = 12;
document.getElementById("totalOrders").textContent = 5;

// Later → will be fetched from API:
// fetch("https://admin/getDashboardInfo")
